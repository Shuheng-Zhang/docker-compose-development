volumes:
  rs-postgres-vol:

services:
  rs-postgres:
    image: postgres:alpine
    container_name: "rs-postgres"
    environment:
      TZ: Asia/Shanghai
      PGTZ: Asia/Shanghai
      POSTGRES_PASSWORD: postgres
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C.UTF-8"
    ports:
      - "5432:5432"
    volumes:
      - rs-postgres-vol:/var/lib/postgresql